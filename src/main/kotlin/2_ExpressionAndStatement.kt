fun main() {
    //expression adalah kombinasi dari operator, fungsi, literal, dan variabel
    var x = 20 % 4
    println(x)

    //operasi assignment tidak bisa dimasukkan di dalam statement
    // karena kotlin akan menganggapnya sebagai statement di dalam statement
    // while ((rem = a % b) != 0) -> error
}